// Standalone build for the platform-independent core (prompts, schema, JSON
// validation, language detection). Kept separate from the Android app module so
// it compiles and tests on a plain JVM -- no Android SDK required.
rootProject.name = "core"
